- name: install package
  hosts: localhost
  become: yes
  tasks:
  - name: install package
    ansible.builtin.dnf:
      msg: "packages installed list: {{item}}"
    loop:
    - mysql
    - nginx
    - postfix  